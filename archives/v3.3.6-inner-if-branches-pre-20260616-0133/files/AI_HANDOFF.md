# AI 协作交接入口

> 如果你刚接手本项目，且不知道从哪里开始，请先阅读项目根目录的 `START_HERE.md`。  
> `START_HERE.md` 是给用户和后续模型使用的唯一记忆入口；本文件负责提供更详细的协作规则。

本文件是后续 AI 模型、文本协作者或代码协作者接手 `witch-shop-game` 时的详细协作说明。目标是减少重复摸索，避免跨模型协作时破坏既有结构。

## 1. 接手前必读顺序

1. `START_HERE.md`：唯一入口，用户只需要记住这个文件。
2. `AI_HANDOFF.md`：当前交接规则与协作规范。
3. `FRAMEWORK.md`：项目模块、调用链、存档与核心系统说明。
4. `docs/V3_ROADMAP.md`：v3.0 阶段路线图与小版本边界。
5. `docs/VERSION_ARCHIVE.md`：版本归档、回退与多模型协作规范。
6. `docs/DATA_CONTRACTS.md`：数据字段、ID、存档兼容契约。
7. `docs/QA_CHECKLIST.md`：修改后的手动验证清单。
8. 如修改故事、角色、礼物、羁绊或内容包，必须阅读 `STORY_SYSTEM_ROADMAP.md`。

## 2. 当前项目阶段

当前项目处于 **v3.0 大版本规划下的 v3.3 故事条件网络、特殊委托与半隐藏预告开题阶段**。

最新上下文：角色故事正文已完成“中文优先”运行时清理；店铺投资故事、金币消费统计、礼物驱动故事条件、故事/羁绊页目标摘要均已接入。`tools/handoff_status.js` 已改为从 `AI_HANDOFF.md` / `CHANGELOG.md` 动态提取当前阶段、推荐下一步与 Unreleased 亮点。2026-06-15 已将 v3.2 校准为“商品介绍 / 商品赠礼 / 一般与特殊 NPC 分层 / 熟客羁绊来访台词 / 羁绊阶段回馈 / 商品样品库存”的最小闭环，详细计划见 `docs/V3_2_PRODUCT_GIFT_SPECIAL_NPC_PLAN.md`。

v3.2 当前已完成的最小闭环：

- v3.2.1 文档与数据契约整理已完成，商品赠礼、特殊 NPC、羁绊回馈与存档兼容边界已写入 `docs/DATA_CONTRACTS.md` 与细化计划。
- v3.2.2 商品赠礼运行时核心已接入：首批商品拥有 `giftable / giftTags / giftBaseExp / giftCost / legacyGiftId / intro` 等字段，羁绊页可把已解锁商品作为“商品样品”赠送，旧 `BOND_GIFTS` 保留为空兼容数组，旧礼物 ID 通过 `legacyGiftId` 与 `BOND_GIFT_LABELS` 兼容。
- 商品首次解锁介绍已接入：`js/modules.js` 手动解锁配方时展示 `intro`，可赠送商品会提示可作为商品样品赠送。
- 商品样品库存已接入：`State.inventory.productSamples` 记录库存，手动解锁 `giftable` 商品时按 `sampleOnUnlock` 发放样品，未配置时默认 1 份；羁绊页赠礼优先消耗样品，无库存时才消耗金币；样品赠礼不计入金币消费统计。
- 商品样品收纳箱已接入：商店页新增“收纳箱”tab，集中展示已解锁可赠送商品、样品数量、基础羁绊收益与样品耗尽后的金币回退成本，用于手动 QA 与玩家自查库存。
- v3.2.3 特殊 NPC 来访第一版已接入：订单生成约 20% 概率生成当前阶段可见的具名熟客，同一时间最多 1 个熟客订单；`SPECIAL_NPC_VISIT_LINES` 为 20 个角色提供 `bond0~bond3` 来访台词；订单卡片显示“普通来客 / 熟客 · 羁绊阶段”。
- 羁绊阶段回馈提示已接入：熟客订单完成后会读取未展示的 `stageReward` 历史并弹出一次“羁绊回馈”toast，随后写回 `toastShown` 避免重复提示。

当前已完成 v3.3.1～v3.3.5 的第一轮故事条件整理与调试辅助：未解锁故事与羁绊页“下个故事”目标摘要已从后台条件清单，改为玩家可理解的半隐藏线索表达；故事解锁读取路径已通过 `requirements` 兼容层统一，运行时优先读取新 `requirements`，同时保留旧 `unlock*` 字段兼容；流失订单叙事入口已完成第一步，`customersLost` 现在会在 1/3/5 次流失时解锁店铺 IF 故事（“柜台边的空纸袋 / 道歉名单 / 口碑危机”）。v3.3.4 进一步加入“第二次机会的窗口”和“后屋的低语账页”，把流失订单、累计金币投入与 `comfort / mystery / wisdom` 店铺气质连接起来，作为经营 IF / 里世界入口的最小故事钩子。v3.3.5 在 `js/game.js` 启动后挂载 `window.WitchDebug` 控制台调试入口，用于快速调整流失顾客、金币、累计消费、店铺气质与统计字段并触发故事检查；该入口不新增正式 UI，仅供本地 QA。v3.2 的礼物部分仍定位为“首批商品样品赠礼最小闭环”：并非所有商品都已进入礼物库，也不追求每个商品拥有 NPC 专属反应。完整礼物库扩充、角色偏好矩阵、礼物触发故事、特殊 NPC 专属订单链、经营 IF 网络与复杂身份助力仍属于 v3.3 或更后续工作。如未来推进 v3.4 多语言词库包，可从英文归档恢复/拆分资源。

核心目标是将游戏从：

```text
扁平经营 + 解锁后阅读故事
```

升级为：

```text
订单表现 → 金币产出/消耗 → 商品解锁/礼物/店铺投资 → 羁绊变化
→ 特殊 NPC 访问反馈/故事解锁/回馈助力 → 新订单/新反馈
```

后续优先方向：

```text
v3.2 商品赠礼与特殊 NPC 关系循环
v3.3 故事条件网络、特殊委托与半隐藏预告
v3.4 内置多语言词库包系统
v3.5 自定义词库导入
```

## 3. 修改前流程

1. 明确本轮目标、范围和成功标准。
2. 判断属于小改动还是大改动。
3. 创建归档：
   - 小改动：备份受影响文件到 `archives/<version>/files/`。
   - 大改动或基线：创建完整可运行快照 `archives/<version>-full/`。
4. 如涉及数据结构，先检查 `docs/DATA_CONTRACTS.md`。
5. 如涉及故事、角色、礼物、羁绊，先检查 `STORY_SYSTEM_ROADMAP.md`。

## 4. 修改后流程

修改完成后至少执行：

```bash
node tools/project_check.js
```

如果本轮只修改运行时代码且想快速定位语法问题，可额外或单独运行：

```bash
node tools/check_syntax.js
```

并根据改动类型参考：

```text
docs/QA_CHECKLIST.md
```

完成后更新：

1. `CHANGELOG.md`
2. 本轮归档目录中的 `MANIFEST.md`

## 5. 不要轻易做的事

- 不要无说明地改 `localStorage` 存档 key：`witchShopSave_v1`。
- 不要直接删除旧字段，例如故事旧字段 `unlockPhase / unlockCoins`。
- 不要重命名已有核心 ID，例如模块 ID、升级 ID、角色 ID。
- 不要删除历史归档目录。
- 不要把大型功能混进文档整理任务。
- 不要让玩家可见线索直接暴露后台字段，例如 `giftId = xxx`。

## 6. 当前推荐下一步

当前建议继续推进：

```text
v3.3：故事条件网络、特殊委托与半隐藏预告
```

封版 QA 已在 `docs/QA_CHECKLIST.md` 中补充“v3.2.4 封版端到端场景”。若需要回看 v3.2 是否可封版，应优先按该段手动检查新存档完整闭环与旧存档兼容，而不是继续扩张 v3.2 新功能。

优先确认已完成的最小闭环：

```text
商品解锁介绍 → 样品库存 → 商品赠礼 → 羁绊提升
→ 熟客来访台词变化 → 羁绊阶段回馈提示
```

当前 v3.3.1～v3.3.2 已先处理：

```text
半隐藏故事预告统一：让未解锁故事显示玩家可理解的线索，不暴露 giftId、unlockStats 等后台字段；羁绊页目标摘要同步使用可读线索。
requirements 兼容层：故事解锁、故事页锁定条件与羁绊页下个故事摘要统一读取 Story.getUnlockConditions() / _getEntryRequirements()，旧 unlock* 字段继续兼容。
```

v3.3.3～v3.3.5 已开始处理：

```text
流失订单叙事入口第一步：新增 1/3 次 customersLost 店铺 IF 故事，并与既有 5 次“口碑危机”组成早期经营失手叙事梯度。
经营 IF / 里世界入口第一步：新增 customersLost + shopTraits + coinsSpentTotal 的组合条件故事，验证失败、投入与店铺气质可以共同推动隐藏店铺线。
调试验证辅助：启动后可在控制台使用 WitchDebug.status() / addLostCustomers() / spendCoins() / setShopTrait() / checkStories() 快速推进组合条件。
```

v3.3 优先解决：

1. 做一次 v3.3.2 后的手动 QA：确认故事页锁定卡与羁绊页“下个故事”摘要在新旧字段上都一致。
2. 对 v3.3.3～v3.3.5 流失订单与经营 IF 故事做手动 QA：确认订单超时会更新 `customersLost`，并按 1/3/5/8 以及店铺气质/累计消费组合条件解锁对应店铺故事；需要快速造数时可使用 `WitchDebug` 控制台入口。
3. 继续扩展经营 IF / 里世界入口最小闭环时，优先复用 `requirements.stats` 与 `requirements.shopTraits`，再考虑商品赠礼、羁绊与熟客来访记录，不要急于新增复杂字段。
4. 礼物库与故事联动扩充：把未进入 `giftable` 的商品按模块、标签和角色故事分批接入；为重点角色设计偏好矩阵、专属反应或礼物触发故事，避免在 v3.2 封版阶段只做无叙事支撑的商品清单扩张。

暂不建议继续在 v3.2 中扩张完整特殊委托链、复杂数值助力或大规模新故事条件字段。
