# Witch Shop Game v3.0 路线图

本文档用于把 3.0 大版本拆分为可执行的小版本计划。`STORY_SYSTEM_ROADMAP.md` 负责长期设计规范；本文档负责 3.0 阶段的实际开发边界、顺序与验收标准。

## 1. 版本定位

### v2.0 基线

上一个稳定大版本，可理解为：基础经营、订单、模块解锁、故事阅读与羁绊雏形已经存在。

### v3.0 总目标

将游戏从“扁平经营 + 解锁后阅读故事”升级为一个互相反馈的经营叙事循环：

```text
订单表现 → 金币产出/消耗 → 商品解锁/礼物/店铺投资 → 羁绊变化
→ 特殊 NPC 访问反馈/故事解锁/回馈助力 → 新订单/新反馈
```

核心解决三个问题：

1. 金币后期缺少持续用途。
2. 商品、故事解锁与经营、羁绊、礼物、订单表现关联不足。
3. 角色目前更多存在于故事阅读页，尚未充分进入订单经营主循环。

同时为后续多语言词库、自定义 NPC、自定义内容包预留结构。

### v3.0 优化后的系统框架（2026-06-15）

v3.0 阶段的重心不再只是“增加金币消耗项”或“扩写故事条件”，而是把商品、NPC 与经营循环打通：

```text
商品系统
  ├─ 商品用于订单与模块成长
  ├─ 新商品解锁时展示介绍与用途
  ├─ 已解锁商品可作为礼物赠送
  └─ 商品标签用于匹配特殊 NPC 偏好

专精系统
  ├─ 模块专精来自长期经营
  ├─ 专精影响对应模块商品的礼品品质
  └─ 礼品品质影响羁绊收益与回馈触发强度

NPC 系统
  ├─ 一般 NPC：性格 + 身份组合，负责稳定订单流与世界氛围
  ├─ 特殊 NPC：已有 20 个具名角色，负责关系经营与长期记忆
  ├─ 羁绊影响特殊 NPC 来店台词、称呼、订单表现与后续故事
  └─ 特殊 NPC 可拥有商品偏好、订单偏好、身份助力与专属反馈

订单系统
  ├─ 一般 NPC 保证基础订单刷新
  ├─ 特殊 NPC 以一定概率作为熟客来访
  ├─ 特殊 NPC 订单卡显示具名身份与羁绊等级
  └─ 后续可扩展特殊委托、异常订单与里世界入口
```

一句话原则：

```text
普通客人让小店热闹，特殊客人让小店有记忆。
```

羁绊的作用也相应扩展为：

```text
解锁故事 + 改变来访台词 + 影响特殊订单/回馈 + 体现 NPC 与小店关系变化
```

---

## 2. 小版本拆分

## v3.1：版本整理与系统基线

### 目标

把当前项目整理为 3.0 开发基线，避免后续功能混杂推进。

### 范围

- 修正版本命名：当前 `v0.2.0` 语义调整为 `v3.1-baseline`。
- 完善 `CHANGELOG.md` 与 `docs/VERSION_ARCHIVE.md`。
- 保留已有归档目录，但在文档中说明其历史命名原因。
- 整合 `STORY_SYSTEM_ROADMAP.md`，补充多语言词库规划。
- 新增 `AI_HANDOFF.md`，作为详细协作交接说明。
- 新增 `START_HERE.md`，作为用户和后续模型的唯一记忆入口。
- 新增 `docs/DATA_CONTRACTS.md`，明确数据字段、ID 与存档兼容契约。
- 新增 `docs/QA_CHECKLIST.md`，补充语法检查之外的手动验证清单。
- 创建完整可运行基线快照与轻量协作文档归档。
- 确认当前语法检查通过。

### 验收标准

- 文档中明确 `v2.0 → v3.0` 的版本关系。
- 3.0 小版本拆分清晰。
- 用户只需要记住 `START_HERE.md`，后续模型/协作者可从该入口接手。
- 后续模型/协作者能通过 `AI_HANDOFF.md`、`CHANGELOG.md`、`docs/V3_ROADMAP.md` 判断当前阶段。
- 数据结构修改前有 `docs/DATA_CONTRACTS.md` 可参考。
- 修改后有 `docs/QA_CHECKLIST.md` 可验证。

---

## v3.1-automation：归档与检查自动化

### 目标

降低多模型协作时用户的记忆负担，把归档、检查、交接提示从“靠人记住”改为“由脚本和入口文档承接”。

### 主要功能

- 新增 `tools/create_archive.js`：自动创建轻量归档或完整快照，并生成 `MANIFEST.md`。
- 新增 `tools/project_check.js`：统一运行语法检查、关键文档检查、归档结构检查。
- 可选新增 `tools/handoff_status.js`：输出可直接复制给后续模型的接手提示。
- 更新 `AI_HANDOFF.md`、`docs/VERSION_ARCHIVE.md`，把自动化流程写入规范。

### 验收标准

- 用户不需要手动记忆归档目录结构。
- 新模型接手时只需要读取 `START_HERE.md`。
- 修改前可以用一条命令创建归档。
- 修改后可以用一条命令完成基础检查。

---

## v3.2：商品赠礼与特殊 NPC 关系循环

### 目标

在已经完成店铺投资、金币消费统计与礼物故事条件样例的基础上，把 v3.2 从“单纯补金币消耗出口”升级为：

```text
商品解锁 → 商品介绍/样品 → 商品赠礼 → 羁绊变化
→ 特殊 NPC 来访台词变化/回馈提示 → 后续故事或订单入口
```

v3.2 的核心不是一次性做完完整角色系统，而是建立一个最小可运行闭环，让玩家能明确感到：

```text
经营获得的商品与金币，会改变自己和具名 NPC 的关系；
羁绊提高后，NPC 不只解锁故事，也会在订单来访时以不同台词回应玩家。
```

详细落地拆解见：

```text
docs/V3_2_PRODUCT_GIFT_SPECIAL_NPC_PLAN.md
```

### 主要功能

- 商品介绍与样品：新商品解锁时展示简短介绍，并可获得少量赠礼样品或赠礼资格。
- 商品赠礼：已解锁商品可作为礼物赠送给特殊 NPC，商品标签与 NPC 偏好决定羁绊收益。
- 专精影响礼品品质：模块专精越高，对应商品作为礼物时品质越高，羁绊收益或回馈概率越好。
- 一般 NPC / 特殊 NPC 分层：
  - 一般 NPC：由“性格 + 身份”组合生成，例如“温柔的旅人”“急躁的商贩”。
  - 特殊 NPC：已有 20 个具名角色，进入熟客池，拥有专属台词、偏好与羁绊表现。
- 特殊 NPC 来访台词：订单生成抽中特殊 NPC 时，根据当前羁绊等级读取不同 `visitLines`。
- 特殊 NPC 订单卡表现：显示角色名、身份/称号、羁绊等级或熟客标记。
- 羁绊阶段回馈提示：羁绊达到阶段后，至少展示一次“该 NPC 提供了什么帮助/回馈”的明确提示；第一版可先记录与展示，不强制接入复杂效果。
- 兼容既有金币循环：保留店铺投资、金币消费统计与礼物故事条件，不推翻已完成的 v3.2 工作。

### 当前状态与边界校准（2026-06-14）

当前 v3.2 已经完成或接近完成的部分：

- 店铺投资与店铺气质：已接入投资页签、`SHOP_INVESTMENTS`、`shopTraits`、`shopInvestments`，并提供少量店铺气质故事闭环。
- 金币消费统计：已接入 `coinSpendStats` 与 `spendCoins()` 分类统计，可区分礼物、羁绊、店铺投资等消费方向。
- 礼物系统深化：已支持送礼统计、指定礼物计数、重复赠礼衰减、`unlockGifts` 与故事/羁绊页目标提示。
- 故事提示补强：已补齐店铺气质、消费统计、送礼条件等目标摘要，作为 v3.2 的少量反馈样例。

需要特别注意：这些故事相关改动只是为了验证“金币选择会改变故事线索”的最小闭环，不代表 v3.2 要继续扩展完整故事条件网络。

### 封版状态（2026-06-15）

v3.2 商品赠礼与特殊 NPC 关系循环已经完成最小闭环：

- v3.2.1 文档与数据契约整理完成。
- v3.2.2 商品介绍、商品赠礼运行时核心、商品样品库存完成。
- v3.2.3 特殊 NPC 来访、熟客订单卡、羁绊阶段台词完成。
- v3.2.4 羁绊阶段回馈提示、样品库存 QA 清单与文档收尾完成。

当前推荐只做手动 QA、体验微调与封版确认；不建议在 v3.2 继续扩张大系统。下一阶段应进入 v3.3：故事条件网络、特殊委托与半隐藏预告。

### 路线再校准（2026-06-15）

在 2026-06-15 的框架讨论后，v3.2 后续不再优先补“经营消耗品 / 风险控制”作为唯一收尾目标，而是先把商品、赠礼、特殊 NPC 与订单来访打通。原因是：

```text
经营消耗品解决金币出口；
商品赠礼与特殊 NPC 解决经营行为如何被角色记住。
```

因此 v3.2 拆成以下实施层级：

```text
v3.2.1 文档与数据契约整理
  - 明确商品、礼物、NPC 分层、羁绊台词、回馈字段。
  - 判断旧 BOND_GIFTS 与商品赠礼的兼容策略。

v3.2.2 商品介绍与赠礼样品最小闭环
  - 商品解锁时有介绍。
  - 已解锁商品可进入赠礼选择。
  - 商品标签影响特殊 NPC 羁绊收益。

v3.2.3 特殊 NPC 来访最小闭环
  - 订单顾客区分一般 NPC 与特殊 NPC。
  - 特殊 NPC 显示具名身份与羁绊等级。
  - 特殊 NPC 台词随羁绊等级变化。

v3.2.4 羁绊回馈提示与版本收尾
  - 羁绊阶段奖励先以提示/记录形式落地。
  - 补充 QA 清单与数据契约。
  - 商品样品库存最小闭环落地：解锁发放、样品优先消耗、无库存金币回退。
  - 完成后进入 v3.2 手动 QA / 封版确认。
```

v3.2 应继续暂缓以下内容到 v3.3 或更后：

- 统一 `requirements` 字段结构。
- 大规模新增经营统计、流失顾客、订单表现等故事条件。
- 半隐藏预告系统重构。
- 表世界 / 里世界 / IF 故事网络扩写。
- 流失订单作为叙事入口的完整实现。
- 每个特殊 NPC 的完整专属订单链。
- 每个商品的 NPC 专属台词。
- 大规模扩充完整礼物库：未纳入 `giftable` 的普通商品暂不在 v3.2 逐一接入，后续应结合 v3.3 故事条件、角色偏好与专属反应分批设计。
- 身份助力的复杂数值效果。

### 原金币消耗目标的保留位置

经营消耗品 / 风险控制仍然有价值，但不再作为当前最优先落地点。建议处理方式：

```text
如果 v3.2.2-v3.2.4 内容量可控：作为 v3.2.5 追加 1-2 个一次性道具。
如果商品/NPC 闭环内容量较大：顺延到 v3.3 前置或独立经济小版本。
```

### 设计原则

- 金币消耗要提供“方向选择”，而不是只做数值黑洞。
- 礼物和商品用于“靠近角色”，店铺投资用于“改变店铺身份”，消耗品用于“改变经营节奏”。
- 一般 NPC 负责订单流量，特殊 NPC 负责关系记忆，二者不能互相取代。
- 羁绊首先影响玩家可见反馈：来访台词、称呼、订单卡表现、回馈提示。
- 特殊 NPC 是增强体验，不应阻塞基础订单刷新。
- 中后期金币增长可以保留，但应让玩家始终有值得等待、规划和取舍的目标。
- 不把流失订单简单定义为口碑惩罚；它也可以是里世界、异常顾客、隐藏委托的入口。

### 验收标准

- 玩家在中后期有稳定金币消耗或商品消耗出口。
- 至少 2-3 类资源用途不是单纯数值膨胀，而能影响羁绊、NPC 反馈或故事。
- 不破坏原有订单收益和模块解锁流程。
- 玩家可以通过商品赠礼主动推进至少一类角色关系线索。
- 至少存在一个“商品赠礼 → 羁绊提升 → 特殊 NPC 来访台词变化”的闭环样例。
- 至少存在一个“金币消费/店铺投资 → 故事预告变化/故事可解锁”的既有闭环样例继续可用。

---

## v3.3：故事条件网络、特殊委托与半隐藏预告

### 目标

让故事从线性解锁变为与经营数据、角色羁绊、商品赠礼、金币消费、特殊 NPC 来访、成就记录和世界状态相关的条件网络。

v3.3 的重点是让故事不再只是“满足数值后出现”，而是让玩家通过半隐藏线索理解：自己和角色的关系、隐秘工坊、职业/角色相关订单记录、礼物与店铺投资，正在打开不同叙事入口。

v3.3.8 后需要特别校准：`another side / side story` 是乙女向角色侧面故事，不是店铺经营失误故事。流失订单可以作为后台判定指标，但不能被写成“因为店铺流失订单，所以角色 side story 发生”的剧情因果。

同时，v3.3 也是礼物库从“v3.2 最小机制闭环”扩展为“故事内容网络”的阶段。v3.2 只验证首批商品可作为样品赠礼、可影响羁绊与旧礼物条件；未进入礼物库的商品不应在 v3.2 封版前仓促全部接入，而应在 v3.3 中按角色故事、商品标签、特殊委托和世界观线索分批扩展。

### 主要功能

- 统一故事字段：`scope`、`route`、`characterIds`、`preview`、`requirements`。
- 支持更多 `requirements`：羁绊、商品赠礼、礼物标签、经营统计、流失顾客、金币消费、特殊 NPC 访问/回馈记录。
- 规划并扩充正式礼物库：决定哪些商品进入 `giftable`，补齐 `giftTags / giftBaseExp / giftCost / intro / sampleOnUnlock`，并避免只做无叙事含义的数值列表。
- 建立角色偏好矩阵：每个特殊 NPC 至少拥有明确的喜欢标签，重点角色逐步补充专属高偏好商品、专属反应或故事触发礼物。
- 将礼物作为故事钥匙：部分故事可由特定商品、特定礼物标签、累计赠礼或角色专属礼物触发，但必须提供玩家可理解的半隐藏线索。
- 锁定故事显示半隐藏式预告。
- 羁绊页显示对应角色的可推进故事线索。
- 特殊 NPC 高羁绊后可出现特殊委托或异常访问。
- 增加若干经营 IF 故事，例如异常委托、贵族顾客、被冷落的熟客、夜间来客。
- 建立“表世界 / 里世界 / 店铺线”三类推进入口：
  - 表世界：常规订单、常规礼物、稳定羁绊。
  - 里世界 / another side：高羁绊、主题礼物、隐秘工坊、角色/职业维度订单记录。
  - 店铺线：累计消费、店铺投资方向、订单结构与顾客反馈。
- 流失订单作为判定指标：全局流失可服务店铺线；职业/角色分类流失应进入 `storyStats` 与成就页，作为 side story 的半隐藏门槛，而非正文因果。
- 隐秘工坊 `private_workroom` 作为 side story / another side 的总入口，开启后可追溯读取既有分类记录。
- 成就页 / 记录页承接职业或角色维度流失阶段，例如“骑士 · 未抵达的委托 I/II/III”，不污染当前统计页。

### 验收标准

- 玩家能通过线索理解故事大致推进方向。
- 至少存在角色故事、店铺故事、IF 故事三种类型。
- 经营失败不只是惩罚，也能产生店铺线或世界线反馈；但角色 another side 不被写成经营失败后果。
- 至少存在一个 `inner / another side` 故事由“隐秘工坊 + 角色羁绊 + 角色/职业分类记录”共同影响。
- 锁定故事的 `preview.visibleHints` 能解释玩家应该从经营、送礼或店铺投资哪个方向尝试推进。
- 职业/角色分类流失计数不进入当前统计页，应由成就页或记录页展示阶段。
- 礼物库扩充以故事和角色关系为导向，至少完成一批商品的 `giftable` 接入、角色偏好匹配与可读提示；不要求一次性覆盖所有商品。

### v3.3.8 框架纠偏（2026-06-16）

已将 `another side` 与店铺经营 IF 做边界拆分：

- 新增 `docs/V3_3_ANOTHER_SIDE_FRAMEWORK.md`，明确 `another side` 是乙女向角色侧面故事，不是店铺经营事故故事。
- `private_workroom` / 隐秘工坊定义为 side story 总入口。
- 规划 `State.storyStats.lostOrdersByRole / lostOrdersByIdentity`，用于记录角色/职业维度流失订单次数。
- 职业/角色流失计数后续进入成就页 / 记录页，不进入当前统计页。
- v3.3.3～v3.3.7 的 9 条全局 `customersLost` 店铺故事保留为历史店铺线与 QA 样例，不再作为 another side 正确样例继续扩张。

后续推荐拆分：

```text
v3.3.9：分类计数运行时 + requirements.storyStats / requirements.shopInvestments 读取
v3.3.10：成就页 / 记录页 UI + 职业/角色阶段成就展示（第一版已接入故事页“记录”视图）
v3.3.11：首批骑士、恶魔、花仙子 another side 样例（已接入）
```

### v3.3.10 记录页第一版（2026-06-16）

已将职业/角色分类记录从纯后台计数推进到可见但半隐藏的记录页雏形：

- `js/data.js` 新增 `STORY_RECORD_MILESTONES`，用配置描述记录 bucket、key、阶段阈值、玩家可读标题与线索文案。
- `js/story.js` 在故事页新增“故事 / 记录”切换；记录页读取 `getStoryStatCount(bucket, key)` 展示阶段进度。
- 首批样例包括骑士 / 旅行者职业记录，以及艾德里安 / 恶魔 / 花仙子角色记录。
- 记录页不进入当前统计数据面板，不直接暴露 `lostOrdersByRole / lostOrdersByIdentity` 字段名。
- 这些记录仅作为成就 / 记录与 another side 前置线索，不把流失订单写成角色侧面故事的剧情因果。

### v3.3.11 首批 another side 样例（2026-06-16）

已将 v3.3.8～v3.3.10 的框架落到首批角色侧面故事：

- `js/data.js` 新增 `knight_another_side_01`、`demon_another_side_01`、`fairy_another_side_01`。
- 三条故事均要求 `private_workroom` / 隐秘工坊、对应角色羁绊 Lv.2 与 `requirements.storyStats` 分类记录。
- 正文聚焦角色关系中的另一面：骑士的职责外守护、恶魔的不伪装交易、花仙子的等待与信任；不把流失订单写成剧情因果。
- 新增 `tools/check_another_side_stories.js` 并接入 `node tools/project_check.js`，自动检查 another side 条件边界与预告字段。

### v3.3.13 QA 模板沉淀（2026-06-16）

已将首批 `another side` 样例从“已接入数据”推进为后续可复制的扩写与验收模板：

- `docs/V3_3_ANOTHER_SIDE_FRAMEWORK.md` 新增后续扩写模板，明确推荐字段、隐秘工坊入口、羁绊条件、分类记录条件与预告写法。
- `docs/QA_CHECKLIST.md` 补充首批样例 QA、锁定提示 QA、记录页联动 QA 与扩写模板检查项。
- 后续新增第二批 `another_side` 故事时，应优先复制该模板并通过 `tools/check_another_side_stories.js`，而不是沿用 v3.3.3～v3.3.7 的店铺 IF 故事结构。
- 当前推荐下一步：先手动验收骑士、恶魔、花仙子三条首批样例，再扩写第二批角色 `another side` 或进入特殊委托链规划。

### v3.3.14 第二批 another side 规划（2026-06-16）

已将第二批角色侧面故事先沉淀为规划稿，避免在主题、条件和 QA 边界未统一前直接扩张运行时数据：

- 正式样例候选为 `vampire_another_side_01`、`necromancer_another_side_01`、`ghost_another_side_01`。
- 延后候选为 `elf_another_side_01` 与 `dragon_another_side_01`，更适合等待礼物标签、成长阶段或特殊委托链进一步明确后接入。
- 第二批继续沿用 `private_workroom`、角色羁绊 Lv.2 与 `requirements.storyStats` 分类记录的组合条件。
- 本轮不新增运行时故事数据、不新增角色 ID、不新增统计 bucket；下一步可在手动验收首批样例后，选择正式接入第二批 3 条数据。

### v3.3.15 第二批 another side 数据接入（2026-06-16）

已将 v3.3.14 的第二批正式样例候选接入运行时故事数据：

- `js/data.js` 新增 `vampire_another_side_01`、`necromancer_another_side_01`、`ghost_another_side_01`。
- 三条故事继续沿用 `private_workroom`、角色羁绊 Lv.2 与 `requirements.storyStats.lostOrdersByRole` 组合条件，不新增角色 ID 或统计 bucket。
- 正文分别聚焦吸血鬼的克制邀约、死灵法师在亡者与活人之间的动摇、幽灵被念出名字后的停留愿望，不把流失订单写成剧情因果。
- QA 重点是自动检查 `node tools/check_another_side_stories.js` / `node tools/project_check.js`，以及手动验证 `WitchDebug.prepareAnotherSide('vampire'|'necromancer'|'ghost')` 后可解锁阅读。

### v3.3.1 开题进展（2026-06-15）

已优先落地“半隐藏故事预告统一”的第一步：

- `js/story.js`：未解锁故事卡片不再只显示后台条件式的标签清单，改为“推进方向 + 可选进度”的玩家可读线索。
- `js/shop.js`：商店→羁绊页的“下个故事”目标摘要同步改为可读线索，避免羁绊页与故事页表达分叉。
- 本轮不新增数据字段，不删除旧 `unlockPhase / unlockCoins / unlockStats / unlockGifts` 等条件字段，只在展示层做兼容增强。

后续 v3.3 可继续推进 `requirements` 统一、流失订单叙事入口、经营 IF / 里世界最小样例与礼物库故事联动扩充。

---

## v3.4：内置多语言词库包系统

### 目标

把打字系统从固定英语词库升级为可选择的内置词库包系统，同时保持网页发布开箱即玩。

### 主要功能

- 新增词库包格式：`packId / language / inputMode / entries`。
- 内置英语词库包。
- 内置小型日文幻想主题词库包，采用 `romaji` 输入。
- 订单系统支持 `input / display / meaning`。
- 设置当前词库，默认仍为英语。
- 加载失败时回退到基础内置词库。

### 暂不做

- 玩家自定义词库导入。
- 原生日文假名输入。
- URL 远程词库加载。

### 验收标准

- 网页发布版本不导入任何外部文件也能正常游玩。
- 英语旧词库兼容。
- 日文词库以“显示日文 + 输入罗马音”的方式可玩。

---

## v3.5：自定义词库导入与本地保存

### 目标

让玩家可以在网页中导入自己的 JSON 词库，并在本地保存。

### 主要功能

- 文件选择器导入 `.json` 词库。
- 词库格式校验。
- 保存到 `localStorage`，大词库后续再考虑 `IndexedDB`。
- 删除已导入词库。
- 导出词库。
- 词库错误提示。

### 验收标准

- 导入错误不会破坏游戏。
- 清空导入词库后仍能回到内置词库。
- 自定义词库不作为网页发布的必要条件。

---

## v3.6：内容包与长期扩展预研

### 目标

为自定义 NPC、故事包、词库包统一导入/导出做准备。

### 主要功能

- 设计内容包结构：`characters / gifts / stories / relationships / wordPacks`。
- 冲突检测：ID、角色引用、礼物引用、故事条件可达性。
- 角色搜索、筛选、排序预研。
- 关系故事界面预研。

### 验收标准

- 有明确数据结构文档。
- 不急于做完整编辑器，先保证结构可扩展。

---

## 3. 当前推荐下一步

当前 **v3.1：版本整理与系统基线**、**v3.1-automation：归档与检查自动化** 已完成，**v3.2 商品赠礼与特殊 NPC 关系循环** 已完成最小闭环，项目已进入 **v3.3 故事条件网络、特殊委托与半隐藏预告** 开题。

推荐下一步优先做：

```text
v3.3.13 首批 another side 手动 QA → 第二批角色 another side 扩写 / 特殊委托规划
```

之后继续：


```text
v3.3 故事条件网络、特殊委托与半隐藏预告
→ v3.4 内置多语言词库包
→ v3.5 自定义词库导入
```

## 4. 范围控制原则

- 不在 v3.1 直接改大功能。
- 商品 / 金币 / 羁绊 / NPC 关系循环优先于多语言系统。
- 内置词库优先于导入词库。
- 日文先做罗马音输入，不做原生 IME 假名输入。
- 所有故事条件必须有玩家可理解的线索。
- 用户不需要记住复杂交接流程；后续模型应从 `START_HERE.md` 自行读取上下文。
- 每轮迭代前后都要更新 `CHANGELOG.md` 与归档说明。